apiVersion: v1
kind: Pod
metadata:
  name: nettools
  namespace: nettools
spec:
  containers:
    - name: nettools
      image: jrecord/nettools:latest
      command:
        - sleep
        - "36000"
      imagePullPolicy: IfNotPresent
  restartPolicy: Never
